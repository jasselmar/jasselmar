import { circularBlack } from "@/app/utils/fonts";
const Header = () => {
    return (
        <header className="flex justify-center items-center p-4">
            <h1 className={`${circularBlack.className} text-5xl text-zeus`} >JM</h1>
        </header>
    )
}

export default Header